const processNumbers = require("/Users/vedranakulic/Desktop/Cloud ops Class/Lesson 3/src/Assignment3.js");

describe(`GIVEN we coded the processNumbers function`, () => {
  describe("WHEN invoking the function", () => {
    it(`THEN it should return {max: 3, sum: 6}`, () => {
      const numbers = processNumbersFunction();
      expect(max, min).toBe(3,6);
    });
  });
});

